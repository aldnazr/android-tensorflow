<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- state_checked = false → tidak aktif -->
    <item android:color="?attr/colorOnPrimaryContainer" android:state_checked="false" />
    <!-- state_checked = true → aktif -->
    <item android:color="?attr/colorOnPrimaryContainer" android:state_checked="true" />
</selector>
